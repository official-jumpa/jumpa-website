// Removed: Outlet not needed in Next.js - use nested routes instead;

const Loan = () => {
  return (
    <div>
      {/* Loan  UI */}
      {/* Routes will render here */}
    </div>
  );
};

export default Loan;
